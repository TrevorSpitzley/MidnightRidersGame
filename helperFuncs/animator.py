class animator:


    def iterateImage(length, spriteArray):
        for i in range(length):
            return 0



    @staticmethod
    def animate(Character, spriteArray, changeTime):
        length = len(spriteArray)




        return 0